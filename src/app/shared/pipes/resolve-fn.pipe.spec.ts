import { ResolveFnPipe } from './resolve-fn.pipe';

describe('ResolveFnPipe', () => {
  it('create an instance', () => {
    const pipe = new ResolveFnPipe();
    expect(pipe).toBeTruthy();
  });
});
